# Formussy

A simple, fast form builder that runs entirely in your browser. Create forms from scratch or start with a template, add fields (text, email, textarea, number, select, checkbox, date), preview the result, and persist everything locally, no backend required.
